type Input = {
    email: string
}

type TFile = {
    file: File,
    name: string,
    previewURL: string,
    size: string,
    progress: number,
    uploaded: boolean,
    error: boolean,
    url: string
}
